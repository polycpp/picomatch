Initial API surface
===================

This placeholder keeps the generated documentation tree buildable during
planning.

Before public release, replace it with one page per public header or logical
module and use Breathe directives to render the documented C++ symbols.
